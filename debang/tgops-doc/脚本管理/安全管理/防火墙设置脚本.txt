#!/bin/bash 

单独添加公司内网
firewall-cmd --permanent --direct --add-rule ipv4 filter INPUT 0 -s $office_ip  -p tcp --dport 22  -j ACCEPT && systemctl restart firewalld

添加名单ipset：
firewall-cmd --permanent --new-ipset=web_whitelist --type=hash:ip

添加IP/段：
firewall-cmd --permanent --ipset=web_whitelist --add-entry="$analysis_hk"

文件添加：
ipset restore -f blacklist.txt  导入ipset规则  



irewall-cmd --get-ipsets   查看有什么名单
ipset del hgcp_whitelist 103.35.118.19   删除单个IP
ipset destroy blacklist  删除ipset   


删除ipset
firewall-cmd --permanent --remove-rich-rule="rule family=ipv4 source address=$black_ip reject"
