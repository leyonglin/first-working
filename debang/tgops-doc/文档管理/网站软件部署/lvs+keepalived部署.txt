

tcp优化+linux文件句柄+自启rc.local+网卡配置+ssh黑白名单连接/etc/hosts.allow[deny]
+禁root登陆/etc/ssh/sshd.config(要提前创管理员账号)
+全局变量/etc/profile
export HISTSIZE=10                  
export HISTTIMEFORMAT="%F %T "      在历史命令前面添加时间戳
export TMOUT=600                    超过600秒终端无操作自动断开
HISTFILE=/root/.commandline_warrior 自定义历史命令存放文件
export HISTCONTROL=ignoredups       去重-连续命令
export HISTCONTROL=erasedups        去重-整个历史命令
export HISTCONTROL=ignorespace  在不想被记住的命令前面输入一个空格
+时间同步+自定义程序目录+防火墙+sudo设置+别名+备份脚本/计划任务 