andboxie(又叫沙箱、沙盘)即是一个虚拟系统程序,允许你在沙盘环境中运行浏览器或其他程序，因此运行所产生的变化可以随后删除,类似沙盒的独立作业环境，在其内部运行的程序并不能对硬盘产生永久性的影响。其为一个独立的虚拟环境，可以用来测试不受信任的应用程序或上网行为
沙盒:限制授予应用程序的代码访问权限,用于为一些来源不可信、具备破坏力或无法判定程序意图的程序提供试验环境。然而，沙盒中的所有改动对操作系统不会造成任何损失。
影子系统（PowerShadow Master）是隔离保护Windows操作系统，同时创建一个和真实操作系统一模一样的虚拟化影像系统。进入影子系统后，所有操作都是虚拟的，所有的病毒和流氓软件都无法感染真正的操作系统。系统出现问题了，或者上网产生垃圾文件，只需轻松的重启电脑，一切又恢复最佳状态

Docker 是一个开源的应用容器引擎，管理系统，提供一组命令
让开发者可以打包他们的应用以及依赖包到一个可移植的容器中，然后发布到任何流行的 Linux 机器上，也可以实现虚拟化。容器是完全使用沙箱机制，相互之间不会有任何接口

宿主机

JDK开发工具包--JRE运行环境--JVM是JRE的一部分




公司主营内容：外包门户网站和在线商城

交互设计是介于产品经理和UI设计师之间的工作岗位

Hadoop分布式文件系统(HDFS)

bugfree:开元免费的bug管理系统

FastDFS是一个开源的轻量级分布式文件系统，它对文件进行管理，功能包括：文件存储、文件同步、文件访问（文件上传、文件下载）等，
解决了大容量存储和负载均衡的问题。特别适合以文件为载体的在线服务，如相册网站、视频网站等等

GraphicsMagick号称图像处理领域的瑞士军刀。 短小精悍的代码却提供了一个鲁棒、高效的工具和库集合，来处理图像的读取、写入和操作，支持超过88种图像格式，包括重要的DPX、GIF、JPEG、JPEG-2000、PNG、PDF、PNM和TIFF。

SWFTools提供了一系列将各种文件转成swf的工具, 该工具集包括用于阅读的SWF文件..[Adobe Flash(swf)[player]:动画播放器]

apache openoffice:是一套跨平台的办公室软件套件,免费，兼容

mysql+nginx+redis

weblogic:WebLogic是美国Oracle公司出品的一个application server，基于JAVAEE架构的中间件，
WebLogic是用于开发、集成、部署和管理大型分布式Web应用、网络应用和数据库应用的Java应用服务器。
将Java的动态功能和Java Enterprise标准的安全性引入大型网络应用的开发、集成、部署和管理之中
java体系：java SE(标准版) java EE(企业版)  java ME（移动版）
javaee三层结构模式:界面层(User Interface layer)、业务逻辑层(Business Logic Layer)、数据访问层(Data access layer)。
                     区分层次的目的即为了 “高内聚低耦合” 的思想
MVC(Model View Controller)开始是存在于桌面程序中的，M是指业务模型，V是指用户界面，C则是控制器
             逻辑、数据、界面显示分离的方法组织代码，将业务逻辑聚集到一个部件里面

ElasticSearch是一个基于Lucene的搜索服务器。它提供了一个分布式多用户能力的全文搜索引擎，
基于RESTful web接口。Elasticsearch是用Java开发的
[ELK是Elasticsearch、Logstash、Kibana的简称，这三者是核心套件，但并非全部
Logstash是一个用来搜集、分析、过滤日志的工具
Kibana是一个基于Web的图形界面，用于搜索、分析和可视化存储在 Elasticsearch指标中的日志数据]

命令arch:x86_64

docker网站：https://hub.docker.com/

网络/连接监控，端口监控，数据库监控，web监控，负载监控


cdn:内容分发网络，尽可能避开互联网上有可能影响数据传输速度和稳定性的瓶颈和环节，使内容传输的更快、更稳定。
     通过在网络各处放置节点cache服务器所构成的在现有的互联网基础之上的一层智能虚拟网络,冗余
     使用户就近获取所需内容，降低网络拥塞，提高用户访问响应速度和命中率。CDN的关键技术主要有内容存储和分发技术
     防止DDOS攻击
dns:域名解析系统,将域名解析程ip，方便客户访问，资源解析记录:A NS CNAME AAAA SPV(SOA授权信息开始)   
     根服务器，一二三，主机站点   FQDN     bind-chroot/named  主配置文件，地址库配置文件     正向解析，反向解析
     特殊解析:DNS轮询,泛域名解析          
              子域授权          分离解析view视图(acl列表)          
     缓存DNS(带宽低  ISP)---解析来源:全局转发/根域迭代
     递归/迭代(根)
jenkins：gitlab+jenkins构建[shell]+web--服务器
nginx代理：正向代理(翻墙)与反向代理(负载均衡)
mysql：
zabbix：LNMP+zabbix    c/s(/Agent)-采集数据--数据库---b/s web管理(LNMP)   SNMP(简单网络管理协议)包含
一个应用层协议（application layer protocol）、数据库模型（database schema）和一组资源对象,用以监测连
接到网络上的设备是否有任何引起管理上关注的情况  
http：超文本传输协议，为了提供一种发布和接收HTML页面的方法，客户端和服务器端请求和应答的标准（TCP）
       URL


ps -e -o 'pid,comm,args,pcpu,rsz,vsz,stime,user,uid' |  sort -nrk5

ether-wake -i  本机卡 目标mac地址
bios系统 wake on lan
ping  && arp -n 目标ip
arp地址解析   dns域名解析

node.js  运行在服务器上的javascript


Kubernetes(k8s)--node(工作节点)--1/多pod---Pause(1)+container(n)
		    nodeport	service（集群名称+port(1)(内网)）
					volume
pod共享一组资源(pid+net+ipc+uts+volumes)

进程PID命名空间  网络NET命令空间  IPC命名空间   挂载MNT命名空间(根目录u)  UTS命名空间  USER命令空间

cacti(绘图)   nagios(自定义脚本,自带少，插件多，报警)  zabbix(支持分布是监控)

man 7 signal   查看kill的信号名称作用
前32种称为不可靠信号，一般用于软件不支持排队后，32种称为可靠信号 一般用于硬件支持排队。
du -shck * 查看大文件

快速部署一个web站点：
django:客户端发送url请求，服务端web服务器负责接收，接收后分配到django的urls(路由系统),urls调用相应的views视图函数
从对应的后端模型中获取数据，返回views，再通过模板加工，将数据返回客户端,
如果不是静态文件，而是一个动态的请求，那么nginx就将请求转发给uwsgi,uwsgi 接收到请求之后将包进行处理，处理成wsgi可以接受的格式，并发给wsgi,wsgi 根据请求调用应用程序的某个文件，某个文件的某个函数，最后处理完将返回值再次交给wsgi,wsgi将返回值进行打包，打包成uwsgi能够接收的格式，uwsgi接收wsgi 发送的请求，并转发给nginx,nginx最终将返回值返回给浏览器。

lvm：(what)逻辑卷管理，Linux环境下对磁盘分区进行管理的一种机制，LVM是建立在硬盘和分区之上的一个逻辑层，来提高磁盘分区管理的灵活性。lvm最大的特点就是可以对磁盘进行动态管理。因为逻辑卷的大小是可以在线动态调整的，而且不会丢失现有的数据。将众多空闲的物理卷(分区转化)组成卷组，再从卷组中创建逻辑卷lv

解释--原理(形成过程)--如何使用--特点

nginx:配置简单，强大正则/http应用分流，对网路稳定性小，客户上传大文件中断，不必从头传起，web负载高且稳定和LNMP，缓存(无过期清理机值，只缓存200，手动配置存储介质)，社区活跃
haproxy：虚拟主机，四七层(mysql)代理，web页面管理，正则与性能中庸，支持多种健康检查session，url,8种负载均衡策略
lvs：配置低且支持正则和动静分离，仅分发无io，稳定且负载能力强

裸设备:无文件系统且应用程序可以读写，快      文件系统，多进程使用

路由跟踪命令:traceroute

EMOS是一个基于CentOS，集成了ExtMail Server(邮件服务器)全部组件的小型Linux系统

Hypervisor是一种运行在物理服务器和操作系统之间的中间软件层，可允许多个操作系统和应用共享一套基础物理硬件，因此也可以看作是虚拟环境中的“元”操作系统，它可以协调访问服务器上的所有物理设备和虚拟机，也叫虚拟机监视器

域名注册商新网申请更改解析记录,以CNAME 别名的方式转交给自有 DNS 服务器处理;由自有的 DNS 负责识别 Web 用户的来
源地址,并通过最近的 CDN 缓存节点向用户分发网页内容。

搭建一台私有 CA 服务器,给网站服务和邮件服务签发数字证书,实现邮件服务和网站服务的数据加密传输。	
关键技术:CA、数据加密、数据解密、加密算法	

fastdfs:tracker一台+storage两组

ansible


簇：一定数量(彼此相关联)
栈(堆叠)：某一组协议的关系以及该组协议的层次结构，up/down依赖关系和上下行消息交互



索引(目录结构/文件名)  扇区映射   超级块(账本存放)
超级块  Inode-map  Sector-map  Inode-array

find / -name "*log*" -a -name "*2018*"  -type f -exec rm -rf {} \;
for i in `seq $n`; do  echo $i; done



面试官下午好，非常高兴参加贵公司的面试，这次来面试贵公司的运维工程师岗位。我叫林乐勇，今年27岁，本科毕业，大学是在山西中北大学，毕业后就职于筷云信息科技股份有限公司，岗位是运维工程师，公司性质是外包公司 ，主要是负责帮助甲方公司软硬件开发，网页设计。平时在在公司，我主要负责的内容是负责维护公司内部服务的正常运行及升级，服务器故报警处理及故障排查，数据库有效日常备份，运维相关文档及报告撰写，并帮助甲方公司搭建环境，负责后期的相关技术支援，擅长使用shell编写相关脚本，服务器管理等，同时能使用相关软件部署企业环境。


tcp/ip：syn   syn ack   ack      
      fin   ack   fin ack    ack  

MySQL-MMM是mysql主主复制管理器，主要用来监控mysql主主复制并做失败转移。
自动的主主Failover切换 2、多个Slave读的负载均衡。无法完全保证数据的一致性，无论何时，只有一个数据库可写。

相关表文件:myisam  表名.frm    表名.MYI     表名:.MYD
         innodb  表名.frm    表名.idb








































